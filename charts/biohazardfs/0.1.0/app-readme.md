# BiohazardFS

BiohazardFS is a self-hosted VFX virtual workspace filesystem foundation. This TrueNAS app runs the server MVP with Postgres metadata and RustFS/S3-compatible object storage.

Keep the service internal until an explicit exposure plan is approved.
